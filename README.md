
# Setup

## generate keys

    ``bash
        ./generateCerts.sh
    ```

## Node JS

    ``bash
        npm init -y
        npm install @grpc/proto-loader
        npm install grpc
    ```

## Python 

    ``bash
        virtualenv -p python3 venv
        pip install grpcio-tools && grpcio
    ```
