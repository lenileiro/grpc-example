language: python
python:
  - "3.6"

services:
  - docker

before_script: 
  - pip install docker-compose
  - pip install codecov
  - python server.py &

script:
  - coverage run --source=test -m pytest && coverage report

after_success:
- coveralls